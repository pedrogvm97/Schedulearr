'use client';

import { SchedulePanel } from '@/components/SchedulePanel';

export default function DashboardPage() {
    return (
        <div className="max-w-[1800px] mx-auto p-4 sm:p-8">
            <SchedulePanel />
        </div>
    );
}
